#include <cstdio>

int main() {
  double a = 3.0;
  double b = 4.0;
  /* TODO: a と b の平均を計算して m に代入せよ (右辺を埋める). */
  double m = a + b / 2;
  printf("average of %f and %f is %f\n", a, b, m);
  return 0;
}
