""" md

#* 応用編: AI (ニューラルネットワーク)

# この章のねらい

* ニューラルネットワークの計算の正体が **行列積 + 活性化関数** であることを体験する。
* これまで並列化・SIMD化・GPU化してきた行列積 (`05`, `10`, `13`) が, そのまま AI の推論・学習になる。

# 前提とする既習トピック

* `03_for_collapse` / `05_speedup` / `13_ilp` (行列ベクトル積・行列積)
* `06_reduction` (勾配・損失の集計)
* (発展) `07`–`10` (GPU)

# 含む問題 (準備中)

* `problems/00_mnist_infer` --- 学習済み重みで MNIST 手書き数字を認識 (forward = 行列積)
* `problems/01_regression` --- 線形/ロジスティック回帰の学習 (勾配降下)
* `problems/02_mlp_train` --- 多層パーセプトロンの学習 (forward + backprop)

# 到達目標

* 「並列化した行列積が, 手書き数字を認識し, さらに学習する」を体験する。
* この先の `19_pinn` (物理をNNで解く) への足がかり。
"""
