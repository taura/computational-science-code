#include <cstdio>

int main() {
  double a = 3.0;
  double b = 4.0;
  double m;
  // BEGIN ANSWER: a と b の平均を計算して m に代入せよ.
  m = (a + b) / 2.0;
  // END ANSWER
  printf("average of %f and %f is %f\n", a, b, m);
  return 0;
}
